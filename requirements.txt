# ─────────────────────────────────────────────
# TRUTHGUARD — Python dependencies
# CPU-only build (suitable for HuggingFace Spaces free tier)
# ─────────────────────────────────────────────

# Deep-learning core (compatible with HF ZeroGPU runtime)
torch>=2.2.0
torchvision
torchaudio

# Model architectures
timm==1.0.3                    # EfficientNet-B4 via timm
transformers==4.40.2           # Wav2Vec2 + Transformer utilities

# Audio processing
librosa==0.10.2
soundfile==0.12.1

# Computer vision
opencv-python-headless==4.9.0.80
Pillow==10.3.0

# Scientific computing
numpy==1.26.4
scipy==1.13.0
matplotlib==3.8.4

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # for FastAPI file uploads

# UI
gradio==4.31.5

# HTTP client (Gradio → FastAPI calls)
httpx==0.27.0

# Utilities
tqdm==4.66.4
pydantic==2.7.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx                          # already listed above, also used for test client
